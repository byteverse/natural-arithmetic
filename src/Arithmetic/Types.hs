{-# language ExplicitNamespaces #-}
{-# language TypeOperators #-}
{-# language DataKinds #-}

module Arithmetic.Types
  ( Nat
  , type (<)
  , type (<=)
  , type (:=:)
  ) where

import Arithmetic.Unsafe (Nat, type (<=))
import Arithmetic.Unsafe (type (<), type (:=:))
